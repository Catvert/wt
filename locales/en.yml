_version: 1

err:
  git_missing: "git not found in PATH"
  git_failed: "git %{args}: %{message}"
  not_a_repo: "%{path} is not inside a git repository"
  no_main_repo: "cannot resolve the main repository"
  branch_in_use: "branch '%{branch}' is already checked out in %{path}"
  local_branch_exists: "local branch '%{branch}' already exists — retry with that name"
  start_point_on_existing: "branch '%{branch}' already exists — a start point (%{from}) only applies to a branch being created"
  spawn_failed: "running: %{command}"
  command_failed: "failed (%{code}): %{command}"
  no_free_port: "no free port from %{base}"
  read_failed: "reading %{path}"
  write_failed: "writing %{path}"
  symlink_failed: "symlink %{from} -> %{to}"
  no_browser: "no browser opener found — open %{url} yourself"
  no_config: "no %{file} in %{path} — create one with `wt init`"
  invalid_config: "%{path} is invalid"
  no_cwd: "cannot read the current directory"
  unknown_worktree: "unknown worktree '%{slug}'"
  config_exists: "%{path} already exists (--force to overwrite)"
  no_completions: "no completion script for '%{shell}'"
  ambiguous: "several worktrees match: %{slugs}"
  cancelled: "cancelled"

label:
  url: "url"
  path: "path"
  branch: "branch"
  commit: "commit"
  state: "state"
  git_changes: "git changes"
  kept: "checkout and state kept"
  kept_with_ports: "checkout, ports and state kept"
  error: "error"
  config: "config"
  main: "main repo"
  root: "root"

ok:
  created: "worktree '%{slug}' created — %{path}"
  started: "worktree '%{slug}' started"
  stopped: "worktree '%{slug}' stopped — %{kept}"
  removed: "worktree '%{slug}' removed"
  config_created: "%{path} created"

hint:
  branch_kept: "branch kept: %{branch} (git branch -d %{branch} to delete it)"
  yes_no: "y/N"
  edit_then_new: "edit it, then: wt new demo"
  dir_exists: "directory %{path} already exists"
  mkdir_failed: "creating %{path}"
  copy_source_missing: "copy source missing: %{path}"
  copy_failed: "copying %{path}"
  cancelled: "cancelled"
  unknown_task: "unknown task '%{task}' (see: wt tasks)"
  no_editor: "no editor found — set WT_IDE or [editor] command"
  no_url: "this project has no URL ([open] url / source missing from %{path})"
  no_matching_url: "no address matches '%{target}' (see: wt open %{slug} --list)"
  invalid_slug: "invalid slug '%{slug}': only [a-z0-9-], like a DNS label"
  bad_set: "--set expects key=value, got '%{value}'"
  no_shell_integration: "shell integration missing — add `eval \"$(wt shell-init %{shell})\"` to your rc file, or use `wt shell %{slug}`"

info:
  creating: "creating worktree (%{branch})"
  creating_from: "creating worktree (%{branch}, from %{from})"
  copying: "copy %{from} -> %{to} (%{mode})"
  no_worktrees: "no worktree in %{path}"
  no_tasks: "no task defined in %{path}"
  session_ended: "session ended"

warn:
  no_up_hook: "no [hooks] up in %{path} — this project has no services to start"
  no_down_hook: "no [hooks] down in %{path} — this project has nothing to stop"

confirm:
  remove_long: "remove worktree '%{slug}'? (pre_rm hooks run, the branch is kept)"
  start_now: "start %{slug}'s services now?"
  terminal: "also open a terminal in %{slug}?"
  remove: "permanently remove worktree '%{slug}'?"

state:
  started: "started"
  stopped: "stopped"

col:
  branch: "branch"
  state: "state"
  ports: "ports"

ui:
  press_enter: "press ENTER to go back"
  preview: "preview"
  actions: "actions"
  help: "help"
  confirmation: "confirmation"
  tasks: "tasks"
  task: "task"
  editor: "editor"
  done: "done"
  failed: "failed: %{error}"
  cancelled: "cancelled"
  busy: "action in progress…"
  action_running: "an action is already running"
  empty_hint: "no worktree — press n to create one"
  running_hint: "↑↓ scroll · action in progress"
  close_hint: "ENTER closes · ↑↓ scroll"
  multi_hint: "TAB toggles · ENTER confirms"
  no_match: "no match — ESC clears the search"
  input_hint: "ENTER confirms · ESC cancels"
  confirm_hint: "o or y confirms · any other key cancels"
  mouse_on: "mouse enabled"
  mouse_off: "mouse disabled — terminal text selection is yours again"
  no_services: "this project has no services ([hooks] up missing)"
  nothing_to_stop: "this project has nothing to stop ([hooks] down missing)"
  no_url: "this project has no URL ([open] missing)"
  no_tasks: "no task defined in wt.toml"
  no_editor: "no editor found — set WT_IDE"
  no_selection: "no worktree selected"
  no_choices: "no choice for \"%{question}\" — question skipped"
  branch_taken: "branch already checked out in another worktree"
  already_checked_out: "already checked out"
  new_branch: "＋ new branch"
  new_branch_hint: "pick where it starts from"
  branch_title: "branch for the new worktree"
  base_title: "branch to start from"
  head_here: "HEAD"
  slug_title: "slug for the new worktree"
  slug_from: "slug for the new worktree (from %{base})"
  slug_for: "slug for %{branch} (ENTER accepts the suggestion)"
  empty_slug: "empty slug — creation cancelled"
  start_options: "start options for %{slug} (key=value, space separated)"
  open_title: "open · %{slug}"
  which_worktree: "which worktree? [1-%{max}, ENTER for 1]"

key:
  navigate: "↑↓ navigate"
  actions: "ENTER actions"
  new: "n new"
  start: "s start"
  stop: "d stop"
  shell: "c shell"
  editor: "e editor"
  browser: "o browser"
  task: "t task"
  remove: "r remove"
  help: "? help"
  quit: "q quit"

help:
  new: "n  create a worktree (branch, start point, then slug)"
  start: "s  start (up hooks)        S  start with explicit options"
  stop: "d  stop (down hooks)"
  shell: "c  shell at the worktree root (exit comes back here)"
  editor: "e  open in an editor"
  browser: "o  open in the browser ([open] url)"
  task: "t  run a wt.toml task"
  remove: "r  remove the worktree     g  refresh"
  mouse_toggle: "m  toggle the mouse (to copy text from the terminal)"
  mouse: "Mouse: click selects, double-click opens; wheel scrolls."
  actions: "ENTER  every action        q  quit"
  picker: "In a menu: type to search (fuzzy, spaces narrow), ↑↓ or ^N/^P move, TAB ticks, ESC clears then closes."
  panel: "Action output shows in a panel (↑↓ scroll, ENTER closes)."
  interactive: "Only the shell, interactive tasks and the editor take the terminal back."

action:
  new: "n · create a worktree"
  start: "s · start"
  start_opts: "S · start with options"
  stop: "d · stop"
  shell: "c · shell in the worktree"
  shell_detail: "exit comes back"
  editor: "e · open in an editor"
  browser: "o · open in the browser"
  task: "t · run a task"
  remove: "r · remove"
  remove_detail: "keeps the branch"

title:
  creating: "creating %{slug}"
  starting: "starting %{slug}"
  stopping: "stopping %{slug}"
  removing: "removing %{slug}"
  browser: "browser · %{slug}"
